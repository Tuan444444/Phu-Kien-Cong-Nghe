@{
    // Gán tiêu đề trang
    ViewData["Title"] = "Đặt hàng thành công";
}

<style>
    /* ==============================
           MÀU CHỦ ĐẠO SHOPEE
        ============================== */
    :root {
        --shopee-orange: #ff6600; /* Màu cam chính */
        --shopee-orange-dark: #e65c00; /* Cam đậm hover */
        --shopee-orange-light: #fff5eb; /* Nền cam nhạt */
    }

    /* Tiêu đề chính */
    h2 {
        color: var(--shopee-orange);
        font-weight: 700;
    }

    /* Khung thông báo */
    .success-box {
        border-radius: 14px;
        background: var(--shopee-orange-light);
        border: 1px solid #ffd1b2;
        padding: 2.5rem;
    }

    /* Nút Shopee */
    .btn-home {
        background-color: var(--shopee-orange) !important;
        border-color: var(--shopee-orange) !important;
        font-weight: 600;
        color: white !important;
        transition: 0.2s;
    }

        .btn-home:hover {
            background-color: var(--shopee-orange-dark) !important;
        }

    /* Icon to hơn nổi bật */
    .icon-success {
        font-size: 4rem;
        color: var(--shopee-orange);
    }
</style>

<div class="container text-center mt-5">

    <!-- Hộp thông báo -->
    <div class="success-box mx-auto" style="max-width: 600px">

        <!-- Icon & Tiêu đề -->
        <div>
            <i class="bi bi-check-circle-fill icon-success"></i>
        </div>

        <h2 class="mt-3">Đặt hàng thành công!</h2>

        <p class="mt-2 fs-5">
            Cảm ơn bạn đã mua hàng tại <strong>Phụ Kiện Công Nghệ</strong>.
            <br />
            Chúng tôi sẽ liên hệ với bạn sớm để xác nhận đơn hàng.
        </p>

        <!-- Nút trở về trang chủ -->
        <a asp-action="Index" asp-controller="Home" class="btn btn-home mt-3 px-4">
            <i class="bi bi-house-door"></i> Về trang chủ
        </a>
    </div>
</div>
